import { SxProps } from '@mui/material';

export const withBackBtnHeadSx: SxProps = {
    position: 'relative',
    mb: 3,
};

export const withBackBtnIconButtonSx: SxProps = {
    position: 'absolute',
    top: -12,
    left: -14,
};

export const withBackBtnTitleSx: SxProps = {
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',

    paddingLeft: 4,
    textAlign: 'center',
};
