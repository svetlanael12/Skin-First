import { SxProps } from '@mui/material';

export const linkForgetPasswordSx: SxProps = {
    display: 'flex',
    justifyContent: 'right',
    width: '100%',
    mt: 1,
};

export const linkRegistrationSx: SxProps = {
    display: 'flex',
    justifyContent: 'center',
    width: '100%',
    mt: 1,
};
